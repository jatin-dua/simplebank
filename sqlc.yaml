version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/query/account.sql"
    schema: "./db/migration/schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: true
